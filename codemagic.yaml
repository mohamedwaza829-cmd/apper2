workflows:
  android-workflow:
    name: Android Build
    max_build_duration: 30
    instance_type: mac_mini_m1
    environment:
      node: latest
    scripts:
      - name: Install dependencies
        script: |
          npm install
          npm install @capacitor/core @capacitor/cli
      - name: Build web assets
        script: |
          npm run build
      - name: Configure and Sync Android
        script: |
          # إجبار الكاباسيتور على القراءة من مجلد dist الصحيح
          npx cap copy android --web-dir=dist || true
          
          if [ ! -d "android" ]; then
            npx cap init apper2 io.codemagic.android --web-dir=dist
            npx cap add android
          fi
          
          # تحديث ملف الإعدادات لمنع الخطأ
          sed -i '' 's/"webDir": "www"/"webDir": "dist"/g' capacitor.config.json || sed -i 's/"webDir": "www"/"webDir": "dist"/g' capacitor.config.json || true
          
          npx cap sync android
      - name: Build Android package
        script: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug
    artifacts:
      - android/app/build/outputs/apk/**/*.apk
